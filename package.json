{
  "name": "siliconflow-image-generator",
  "version": "1.0.0",
  "description": "SiliconFlow Image Generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp server.js dist/",
    "serve": "NODE_ENV=production node dist/server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "axios": "^0.21.1",
    "compression": "^1.7.4",
    "helmet": "^4.6.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 